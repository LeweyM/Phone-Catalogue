// renders without error

// renders component x when passed component x as props

// renders component only when props.shouldRender === true

// calls 'togglePopup' callback when click outside of main container
